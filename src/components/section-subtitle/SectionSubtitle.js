import React from "react";

import "./SectionSubtitle.scss";

const SectionSubtitle = ({ subtitle }) => (
  <p className="pt-5 text-center m-1 section__subtitle">{subtitle}</p>
);

export default SectionSubtitle;
