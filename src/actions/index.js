import { setUser, setIsRegisterActive } from "./user/user-actions";
import { toggleCart } from "./cart/cart-actions";

const actions = {
  setUser,
  setIsRegisterActive,
  toggleCart,
};

export default actions;
