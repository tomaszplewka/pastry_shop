import { setUser, setIsRegisterActive } from "./user/user-actions";
import { toggleCart, addToCart } from "./cart/cart-actions";

const actions = {
  setUser,
  setIsRegisterActive,
  toggleCart,
  addToCart,
};

export default actions;
